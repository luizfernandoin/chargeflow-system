version: "3.9"

services:
  postgres:
    image: 192.168.56.10:5000/postgres:15-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=chargerdb
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - chargeflow-network
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role==manager

networks:
  chargeflow-network:
    external: true

volumes:
  postgres_data:
    driver: local